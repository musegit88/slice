"use client";

import { useEffect, useRef, useState } from "react";
import useCategory from "@/hooks/useCategory";

// const CategoryList = ({ filteredCategory }) => {
// const handleDelete = async (categoryId) => {
//   await fetch(`/api/admin/category/${categoryId}`, {
//     method: "POST",
//     body: JSON.stringify(),
//     headers: { "Content-Type": "application/json" },
//   });
// };

// return (

//   );
// };

// export default CategoryList;
{
  /* <div className="flex flex-col gap-4 border p-2 rounded-md"> */
}
{
  /* {categories.map((category) => (
            <div
              ref={ref}
              key={category.id}
              className="flex items-center justify-between border p-1 rounded-md bg-gray-500/10"
            >
              <span ref={ref}>{category.id}</span>
              <span>
                <input disabled type="text" value={category.name} />
              </span>
              <div className="flex items-center gap-x-2">
                <button className="bg-primary text-white px-4 py-2 rounded-md disabled:opacity-50">
                  Edit
                </button>
                <button
                  onClick={() => handleDelete(ref.current)}
                  className="bg-secondary text-white px-4 py-2 rounded-md disabled:opacity-50"
                >
                  Delete
                </button>
              </div>
            </div>
          ))} */
}
{
  /* </div> */
}
